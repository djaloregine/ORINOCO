













/*class Commandes {
    constructor(quantiteMinimale, quantiteActuelle, fournisseur, courriel) {
        this.quantiteMinimale = quantiteMinimale;
        this.quantiteActuelle = quantiteActuelle;
        this.fournisseur = fournisseur;
        this.courriel = courriel;
    }

    alerte() {
        if (quantiteActuelle === quantiteMinimale) {
            return `${this.nomArticle} va être manquant.`;
        }
    }
    commande() {
        let commandeArticle = quantiteStockIdeale - quantiteActuelle;
        return `Envoyer à ${this.courriel} correspondant à ${this.fournisseur} une commandeArticle de ${commandeArticle}`;
    }
}*/